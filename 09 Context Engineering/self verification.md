Task: Write a Java method to parse ISO date strings to LocalDate safely.

Context:
- Java 17
- No external libs
- Input can be null, empty, or invalid

Process:
1) Produce the method implementation.
2) Then run a checklist:
   - Handles null/empty
   - No exceptions leak
   - Includes 3 example inputs with expected outputs
3) If any checklist item fails, revise the method.

Return: final method + checklist result.
