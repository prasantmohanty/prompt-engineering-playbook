Task: Generate release notes in the style shown below.

Examples:
Changes:
- Added Retry-After header support for rate-limited requests
- Improved JWT validation logging
Output:
"Release Notes:
• Added support for Retry‑After headers to help clients handle 429 responses more gracefully.
• Improved JWT validation logs to simplify debugging and enhance security visibility."

Changes:
- Migrated payment routing to v2 backend
- Reduced report generation latency by 30%
Output:
"Release Notes:
• Switched payment routing to the v2 backend for improved stability.
• Optimized reporting service, resulting in a 30% reduction in latency."

Now generate:
Changes:
- Introduced API key rotation policy
- Fixed intermittent 503s in the analytics pipeline
Output:
