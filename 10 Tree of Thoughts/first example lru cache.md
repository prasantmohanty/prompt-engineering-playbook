Use Tree-of-Thoughts.
Generate 3 different approaches for implementing an LRU Cache in Java (no libraries).
Approach types:
1) LinkedHashMap override
2) Custom doubly-linked list + HashMap
3) Array-based circular buffer (if possible)

Then evaluate each approach for:
- Time complexity
- Memory usage
- Code simplicity

Finally select the best approach and provide only the final code.
